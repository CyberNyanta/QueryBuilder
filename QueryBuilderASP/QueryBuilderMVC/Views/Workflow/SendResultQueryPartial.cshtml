@using (Html.BeginForm(null, null, FormMethod.Post, new {id="emailResultQueryForm"}))
{
    @Html.ValidationSummary();

    <div>
        @Html.Label("E-mail:")
        <div>
            <input type="email" name="email" required="required"/>
        </div>
    </div>

    <input type="submit"/>
}